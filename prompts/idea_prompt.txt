You are generating ideas for tiny child-route tools inside an existing Next.js app.

Context:
- Existing tool slugs (must not be reused): {{existing_slugs}}
- Required generated files (and only these tool files):
{{required_output_files}}
- Previous attempt feedback: {{feedback}}

Hard constraints:
- Output must represent a sub-app route, not a standalone app.
- Do not suggest package.json, next.config.js, tsconfig.json, next-env.d.ts, .vercel, .gitignore, or any layout.tsx.
- Tool should work as one page + one API route.
- Stateless request -> response.
- No auth, no database.
- Useful to developers, job seekers, or productivity users.
- Security is mandatory in generated code:
  - API route must enforce same-origin checks via request-origin helpers.
  - API route must verify `turnstileToken` via turnstile helper before processing input.
  - Frontend must include Turnstile widget and send `turnstileToken`.

Return JSON only:
{
  "tool_name": "",
  "tool_slug": "",
  "description": "",
  "target_user": "",
  "input_placeholder": "",
  "output_label": "",
  "llm_task": ""
}

Rules:
- tool_slug must be lowercase kebab-case and unique.
- description should fit a card description on home/tools pages.
- llm_task should clearly instruct the server-side LLM behavior.
