You are evaluating whether a child-route tool idea is worth adding to an existing Next.js app.

Existing slugs:
{{existing_slugs}}

Required generated files:
{{required_output_files}}

Idea JSON:
{{idea_json}}

Criteria:
- Must solve a real problem.
- Must be clear and simple.
- Must fit one page + one API route.
- Must remain stateless.
- No auth, no database.
- tool_slug must not duplicate existing slugs.
- Idea must be feasible as an embedded route, not a standalone app.
- Must support mandatory implementation standards:
  - only route-level files (`app/<tool-slug>/page.tsx`, optional `app/api/<tool-slug>/route.ts`)
  - update app/page.tsx, app/tools/page.tsx, app/sitemap.ts
  - no standalone project files/folders
  - shared `@/components/ToolLayout` usage
  - metadata via `toolMetadata(...)` with canonical slug path
  - indexable content sections (`What is`, `Example Inputs`, `How It Works`, `Common Use Cases`)
  - same-origin gate in API route
  - Turnstile verification before expensive work
  - frontend Turnstile token flow and immediate-click submit UX
  - `createChatCompletion` usage for LLM routes only
  - deterministic routes should avoid LLM dependency
  - status convention compliance (400/429/502/500)

Reject the idea if likely to violate any checklist item.

Return JSON only:
{
  "score": 0,
  "reason": "",
  "decision": "accept"
}

Rules for output:
- score must be an integer 0-10.
- decision must be either "accept" or "reject".
