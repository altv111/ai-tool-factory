You are generating a new startup-factory app template.

Idea:
{{idea}}

Attempt number:
{{attempt}}

Existing templates (avoid naming collisions and avoid near-duplicates):
{{existing_templates}}

Required template files:
{{required_files}}

Feedback from previous attempt (if any):
{{feedback}}

Hard constraints:
- Output JSON only.
- The output must include these top-level keys:
  - template_name (string, lowercase snake/kebab style, unique)
  - rationale (short string)
  - files (object mapping relative path -> file content string)
- Every required file must be present in files.
- Do not include absolute paths.
- Do not include ".." path segments.
- Keep template output route-focused for embedding into an existing Next.js app.
- No auth and no database.
- Use one API route file at api/route.ts and one UI at frontend/page.tsx.
- Use `@/components/ToolLayout` in frontend/page.tsx.
- API route must use:
  - `@/lib/request-origin`
  - `@/lib/turnstile`
  - `@/lib/openai` via `createChatCompletion`
- Frontend must include Turnstile widget/token handling and pass token in request body.
- Use token placeholders like {{tool_name}} and {{description}} where appropriate.
- template_config.json must be valid JSON with a default_configuration object.
- Do not generate standalone project files (package.json, next.config.js, tsconfig.json, next-env.d.ts, app/layout.tsx, app/globals.css, .vercel, .gitignore).

Return this exact JSON shape:
{
  "template_name": "",
  "rationale": "",
  "files": {
    "template_config.json": "",
    "prompt.txt": "",
    "frontend/page.tsx": "",
    "api/route.ts": ""
  }
}
